language: python

python:
  - "2.7"

before_install:
  - pip install .

script:
  - python -m benchpress.testing

deploy:
  provider: script
  script: python setup.py sdist && twine upload dist/*
  on:
    branch: master
